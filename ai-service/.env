# Local environment for AI service
# IMPORTANT: This configuration is for local development only.
# Point this to the SAME MySQL database as the backend for consistent authentication.

APP_NAME=IntelSense AI
ENVIRONMENT=development
API_PREFIX=/api/v1
APP_URL=http://localhost:3000

# Models: real models are enabled by default; set to true only for explicit smoke tests
USE_DUMMY_MODELS=false

# Database: Must match backend database for user consistency
# Note: the @ in the password must be URL-encoded as %40
DATABASE_URL=mysql+aiomysql://Tanishg16:Tanish%402009@localhost:3306/intelsense_ai

# Redis for caching
REDIS_URL=redis://localhost:6379/0

# Email delivery via Gmail SMTP (Mail app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=gadakhtanish@gmail.com
SMTP_PASSWORD=oiuq swzl aifh gnrf
SMTP_USE_TLS=true
ALERT_EMAIL_FROM=gadakhtanish@gmail.com
ALERT_EMAIL_TO=gadakhtanish@gmail.com

# JWT Security
JWT_SECRET=change-me-local-intelsense-ai-jwt-signing-key-2026
JWT_ALGORITHM=HS256
MODEL_CACHE_DIR=/models
LOG_LEVEL=INFO
